# Contributing

- Read `.specify/memory/constitution.md` and `AGENTS.md`.
- Open an issue referencing a spec (`specs/<id>`) or propose a new spec via PR before large changes.
- Conventional Commits; one logical change per PR; tests required.
- Architectural changes need an ADR in `docs/adr/`.
- Be kind. Assume good intent.
